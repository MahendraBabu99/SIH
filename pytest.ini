[pytest]
testpaths = tests
markers =
    browser_flow: jsdom browser-level frontend flow coverage with mocked network/SSE
norecursedirs =
    .*
    build
    dist
    CVS
    _darcs
    {arch}
    *.egg
    .venv
    venv
    node_modules
    local_tests
